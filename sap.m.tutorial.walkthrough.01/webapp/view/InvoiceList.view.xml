<mvc:View
    controllerName="ui5.walkthrough.controller.InvoiceList"
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:mvc="sap.ui.core.mvc">
    <List
        headerText="{i18n>invoiceListTitle}"
        class="sapUiResponsiveMargin"
        width="auto"
        items="{invoice>/Invoices}">
        <headerToolbar>
          <Toolbar>
            <Title text="{i18n>invoiceListTitle}"/>
            <ToolbarSpacer/>
            <SearchField 
               width="50%" 
               search=".onFilterInvoices"/>
          </Toolbar>
        </headerToolbar>
        <items>
            <ObjectListItem
                core:require="{
                    Currency: 'sap/ui/model/type/Currency'
                }"
                title="{invoice>Quantity} x {invoice>ProductName}"
                number="{
                    parts: [
                        'invoice>ExtendedPrice',
                        'view>/currency'
                    ],
                    type: 'Currency',
                    formatOptions: {
                        showMeasure: false
                    }
                }"
                numberUnit="{view>/currency}"
                numberState="{= ${invoice>ExtendedPrice} > 50 ? 'Error' : 'Success' }">
                <firstStatus>
                    <ObjectStatus
                        core:require="{
                            Formatter: 'ui5/walkthrough/model/formatter'
                        }"
                        text="{
                            path: 'invoice>Status',
                            formatter: 'Formatter.statusText.bind($controller)'
                        }"/>
                </firstStatus>
            </ObjectListItem>
        </items>
    </List>
</mvc:View>

<!--
Visualizzazione Avanzata con Formattatori (InvoiceList.view.xml):
- importa il modulo formatter personalizzato tramite "core:require" assegnandogli l'alias "Formatter".
- introduce la proprietà "numberState" nell'ObjectListItem usando l'Expression Binding per cambiare colore al prezzo (Rosso/Errore se > 50, Verde/Successo altrimenti).
- aggiunge l'aggregazione "firstStatus" con un controllo "ObjectStatus" per mostrare lo stato della fattura.
- collega la funzione "Formatter.statusText" alla proprietà "text" dello stato, utilizzando ".bind($controller)" per garantire che il formattatore possa accedere correttamente al controller e ai suoi modelli.
- permette una traduzione dinamica dei codici di stato tecnici in testi leggibili dall'utente tramite il bundle i18n.
-->