<mvc:View
	controllerName="ui5.walkthrough.controller.App"
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc"
	displayBlock="true">
	<Shell>
		<App class="myAppDemoWT">
			<pages>
				<Page title="{i18n>homePageTitle}">
					<content>
						<mvc:XMLView viewName="ui5.walkthrough.view.HelloPanel"/>
            <mvc:XMLView viewName="ui5.walkthrough.view.InvoiceList"/>
					</content>
				</Page>
			</pages>
		</App>
	</Shell>
</mvc:View>

<!--Struttura della UI con Layout (App.view.xml):
- imposta "displayBlock=true" nella View per permettere all'app di occupare l'intera altezza dello schermo.
- utilizza il controllo radice <App> per gestire automaticamente i meta-tag mobile e le future animazioni di navigazione.
- inserisce una <Page> nell'aggregazione "pages" per creare una schermata con un'intestazione (header) e un titolo localizzato.
- utilizza un <Panel> all'interno del "content" della pagina per raggruppare visivamente i controlli correlati (Input e Button).
- mantiene i collegamenti (binding) ai modelli JSON e i18n per la gestione dinamica di testi e dati.
-->