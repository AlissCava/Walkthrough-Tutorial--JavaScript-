<mvc:View controllerName="ui5.walkthrough.controller.App"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc">
    <Button 
      text="Say Hello"
      press=".onShowHello"/>
    <Input 
      value = "{/recipient/name}"
      description = "Hello {/recipient/name}"
      valueLiveUpdate = "true" 
      width = "60%"/>
</mvc:View>


<!--Implementazione Data Binding (App.view.xml):
- aggiunge il controllo <Input> per permettere l'inserimento di dati da parte dell'utente
- introduce la sintassi delle parentesi graffe {…} per il "Data Binding" dichiarativo
- collega (bind) la proprietà "value" al percorso "/recipient/name" nel modello JSON
- utilizza il binding anche nella proprietà "description" per mostrare un'anteprima dinamica
- imposta "valueLiveUpdate=true" per sincronizzare i dati nel modello ad ogni singolo tasto premuto
- definisce la larghezza del campo di input tramite la proprietà "width"
-->